##Elasticsearch
```
暂未测试出问题
```
##Postgresql
```

```
##Druid
```

```
##其他
```
 1. 框架处关于错误返回时（关闭DEBUG模式下，不能只用django的HttpResponseBadRequest 返回，（使用的是html,格式，仍需自己封装）,内部报错有的还是没有封装好，直接暴露，需整改 --》zhang todo
 2. 整理了相关小bug，并修改后，进行试运行测试，发现以下问题，需要具体讨论  -- by zhang 
 （2.1） 对druid进行测试，在测试数据源中，时间字段 __time 不能通过check.py的格式检验，具体检查的标准要进一步讨论，是否有特殊情况
 （2.2） 作为api,协议查询时，查询方式是自由的，例如，用户传输的time结构，可能不完整，使用 time['column'] 默认用户已经传输，容易导致程序问题，使用 len(time.get('column',""),可以同时判断用户传的column为空，或者 column=""的情况
        或者用户可能就仅仅对字段进行查询（api模式下），这个也是不需要进行metric,需要注意这种情况
  -----
  (2.3) 对druid进行测试时，
  如该句查询
  SELECT COUNT(comment) as "mycomment" FROM "wikipedia" GROUP BY namespace 在druid客户端查询，可以正常使用
  然后 协议格式大致如下
  --
  {
  "from":"source1.wikipedia",
  "time":{
      "column":""
  },
  "select":[
    {
      "column":"comment",
      "alias":"mytime",
      "metric":"count"
    }
  ],
  "where":[
   
  ],
  "group":["namespace"]
  --
  程序 pydruid对象出现以下问题
  --
  pydruid.db.exceptions.ProgrammingError: SQL parse failed (org.apache.calcite.sql.parser.SqlParseException): Encountered "None" at line 1, column 79.
Was expecting one of:
    "ALL" ...
    <UNSIGNED_INTEGER_LITERAL> ...
    <APPROX_NUMERIC_LITERAL> ...
    <DECIMAL_NUMERIC_LITERAL> ...
    "?" ...
    --
    
    本人尝试着查阅网上资料，暂时没有找到解决办法，需进一步讨论
```